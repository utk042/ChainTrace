# Low-memory dependency set — everything except PyTorch, PyTorch Geometric
# and SHAP. Install this instead of requirements.txt on hosts with under
# ~1.5 GB of RAM (Render's free tier, small VMs, air-gapped analyst laptops)
# and set CT_LIGHT_MODE=true.
#
# What you give up: the neural autoencoder becomes a PCA linear autoencoder,
# Node2Vec embeddings become structural embeddings, and SHAP attribution
# becomes per-feature reconstruction error. Ingestion, the entity graph,
# Louvain clustering, every structural pattern detector (peel chains,
# CoinJoin, consolidation hubs), risk propagation and the whole API surface
# are unaffected — they never depended on torch.
#
# Installed size: ~120 MB, against ~2.5 GB for the full set.

fastapi>=0.115.0
uvicorn[standard]>=0.30.0
duckdb>=1.1.0
pydantic>=2.9.0
pydantic-settings>=2.5.0
networkx>=3.3
python-louvain>=0.16
scikit-learn>=1.5.0
numpy>=1.26.0
pandas>=2.2.0
python-multipart>=0.0.9
maxminddb>=2.6.0
geoip2>=4.8.0
lxml>=5.3.0
aiofiles>=24.1.0
httpx>=0.27.0
