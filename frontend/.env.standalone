# Build-time settings for `npm run build:standalone` — the single-file build.
# It has no server to map routes onto index.html and no backend to call, so it
# routes through the hash and starts in offline snapshot mode.
VITE_HASH_ROUTER=true
VITE_DEMO_MODE=true
