# Byte-compiled / optimized / DLL files
__pycache__/
*.py[cod]
*$py.class

# C extensions
*.so

# Distribution / packaging
.Python
build/
develop-eggs/
dist/
downloads/
eggs/
.eggs/
lib/
lib64/
parts/
sdist/
var/
wheels/
share/python-wheels/
*.egg-info/
.installed.cfg
*.egg
MANIFEST

# Virtual Environments
.venv/
venv/
ENV/
env/

# Environment Variables
.env
.env.local

# Databases & Cache
*.duckdb
*.duckdb.wal
data/*.duckdb
data/*.duckdb.wal
backend/data/*.duckdb
backend/data/*.duckdb.wal
backend/data/uploads/*
!backend/data/uploads/.gitkeep

# Trained model artifacts (autoencoder, Node2Vec embeddings) regenerate on
# the first pipeline run (~1-3 min for the default 5K-record demo set) and
# are keyed to the current feature schema / dataset's node ids — not worth
# shipping as a multi-MB binary that goes stale the moment either changes.
backend/data/models/*
!backend/data/models/.gitkeep

# Node / Frontend
node_modules/
npm-debug.log*
yarn-debug.log*
yarn-error.log*
frontend/dist/
frontend/dist-ssr/
frontend/.local

# OS generated files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db

# IDE
.vscode/
.idea/
*.swp
*.swo
